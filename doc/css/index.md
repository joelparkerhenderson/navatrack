# CSS

Edit file `assets/css/app.css`.

Append anything you want as any Tailwind layer you want such as:

```css
/* This file is for your main application CSS */
@layer base {
  h1 {
    font-size: var(--text-2xl);
  }
  h2 {
    font-size: var(--text-xl);
  }
}
```
